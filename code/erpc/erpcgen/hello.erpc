/*! 定义项目名称,也是所有生成的源码文件名前缀 */
program hello

#include <stdint.h>

enum lockErrors_t
{
  lErrorOk_c = 0,
  lErrorOutofMemory_c,
  lErrorMaxError_c
}

/*! 定义服务接口函数 */
interface DEMO {
    hello_world() -> void
    say_message(string msg) -> void
    erpc_add(int32 data1, int32 data2) -> int32
}